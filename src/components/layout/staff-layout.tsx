import { Link, Outlet } from 'react-router-dom'
import { useAuth } from '../../features/auth/auth-provider'
export function StaffLayout() { const { user, signOut } = useAuth(); return <div className="site-shell"><header className="topbar"><Link className="brand" to="/panel"><span className="brand-mark">A</span><span>Atiende / Operaciones</span></Link><nav aria-label="Navegación interna"><span className="user-identity">{user?.full_name} · {user?.role}</span><Link to="/panel">Panel</Link><Link to="/panel/tickets">Bandeja</Link><Link to="/panel/conocimiento">Conocimiento</Link><button className="link-button" type="button" onClick={() => { void signOut() }}>Cerrar sesión</button></nav></header><div className="academic-notice">Entorno interno de prototipo · No ejecuta operaciones bancarias</div><main className="page-container"><Outlet /></main></div> }
