import { Link, Outlet } from 'react-router-dom'
import { useAuth } from '../../features/auth/auth-provider'
export function PortalLayout() { const { user, signOut } = useAuth(); return <div className="site-shell"><header className="topbar"><Link className="brand" to="/cliente"><span className="brand-mark">A</span><span>Atiende / Cliente</span></Link><nav aria-label="Navegación del cliente"><span className="user-identity">{user?.full_name} · {user?.role}</span><Link to="/cliente">Resumen</Link><Link to="/cliente/tickets">Mis tickets</Link><button className="link-button" type="button" onClick={() => { void signOut() }}>Cerrar sesión</button></nav></header><div className="academic-notice">Sesión de demostración · El backend validará los permisos</div><main className="page-container"><Outlet /></main></div> }
